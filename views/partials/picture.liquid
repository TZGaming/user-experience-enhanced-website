{% comment %}
  Partial voor Directus afbeeldingen in de snappmap
{% endcomment %}

<picture>
  <img 
    src="{{ imagesrc }}" 
    alt="{{ alt }}" 
    width="200" 
    height="200" 
    loading="lazy">
</picture>